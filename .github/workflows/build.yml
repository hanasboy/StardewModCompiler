using System;

namespace SmartEconomyMod
{
    public class ModEntry : IModInterface
    {
        public void Entry(object helper)
        {
            Console.WriteLine("SmartNPCAndEconomy: Loaded Successfully!");
        }
    }

    public interface IModInterface 
    { 
        void Entry(object helper); 
    }
}
